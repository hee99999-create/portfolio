# 작업요청서 — Career Data Account 웹앱 UI/UX 목업 디자인

> 아래 전체를 복사해 새 Claude 대화(디자인용)에 붙여넣으세요.
> 요청서는 자립형입니다 — 별도 첨부 없이 이것만으로 디자인이 가능합니다.

---

## 0. 당신에게 요청하는 것 (한 줄)

**"Career Data Account"** 라는 웹 서비스의 **전체 유저 플로우와 모든 화면의 고충실도 UI/UX 목업**을,
아래에 확정된 디자인 시스템 안에서 설계해 주세요.

산출물은 **화면별 인터랙티브 HTML 목업**(단일 파일, 순수 HTML/CSS/최소 JS)을 원칙으로 합니다.
각 화면은 데스크톱 우선 + 모바일(390px) 대응.

---

## 1. 제품이 무엇인가 (맥락)

**한 줄 정체성**: "활동 목록이 아니라, **증거 있는 역량 자산**."

대학생이 자신의 경험(프로젝트·활동)의 **링크를 붙여넣으면**, AI가 그 원본에서 역량을 추출하되
**반드시 원본의 정확한 인용(증거)에 근거해서만** 역량을 부여합니다. 증거가 없으면 그 역량은
버립니다(할루시네이션 차단). 추출된 역량은 K-CESA·NCS(한국 표준) 체계로 매핑되어
"역량 자산 카드"로 축적됩니다. 입학에서 졸업까지 학생의 성장을 증거와 함께 자산화하는 제품입니다.

**차별화**: 기존 대학 비교과/마일리지 시스템은 "활동의 양"만 PDF로 쌓습니다. 우리는
① 링크만으로 자동 추출 ② 모든 역량에 증거 인용 ③ 학생이 데이터의 주인. 이 세 가지가 다릅니다.

**사용자 3역할**: 학생(기본) / 교수(동의 기반 열람) / 관리자(집계 통계). 이번 목업은 **학생** 흐름 중심.

---

## 2. 브랜드 & 디자인 시스템 (★ 반드시 이 토큰 안에서. 새 색·폰트 도입 금지)

이 프로젝트에는 이미 확정된 랜딩페이지 디자인이 있습니다. **그 언어를 모든 화면으로 확장**하세요.
새로운 비주얼 언어를 발명하지 말고, 아래 시스템을 일관되게 적용하세요.

### 색 (OKLCH 기반 3개 램프)
```css
:root {
  /* 먹(ink) — 텍스트 */
  --ink-900: oklch(22% 0.04 250); --ink-700: oklch(40% 0.04 250);
  --ink-500: oklch(55% 0.035 250); --ink-400: oklch(65% 0.025 250);
  --ink-300: oklch(78% 0.018 250);
  /* 종이(paper) — 배경/서피스 */
  --paper-50: oklch(98.5% 0.008 80); --paper-100: oklch(96.5% 0.013 80);
  --paper-200: oklch(94% 0.018 80); --paper-300: oklch(89% 0.022 80);
  --paper-400: oklch(82% 0.025 80);
  /* 금박(gold) — accent (절제해서) */
  --gold-700: oklch(48% 0.12 65); --gold-600: oklch(58% 0.13 70);
  --gold-500: oklch(68% 0.12 75); --gold-200: oklch(88% 0.06 80);
  --gold-100: oklch(94% 0.035 80);
  /* 시맨틱 */
  --bg: var(--paper-100); --bg-elev: var(--paper-50); --surface: var(--paper-200);
  --text-primary: var(--ink-900); --text-secondary: var(--ink-700); --text-muted: var(--ink-500);
  --border: var(--paper-300); --accent: var(--gold-700); --accent-bg: var(--gold-100);
}
```

### 타이포그래피
- 제목(serif): **Newsreader** — `font-family: 'Newsreader', Georgia, serif;` (Google Fonts)
- 본문(sans): **Pretendard** — `'Pretendard Variable', system-ui, sans-serif;` (CDN: `https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.css`)
- h1 `clamp(2.5rem, 5.5vw, 4.5rem)` 400 / h2 `clamp(2rem,3.8vw,3rem)` 400 / h3 500
- body line-height 1.7, letter-spacing -0.01em

### 질감 & 모양
- 종이 질감 오버레이(fractalNoise, opacity 0.04, multiply) — 미묘하게.
- radius: sm 4 / md 6 / lg 10 / xl 14 / full 9999. container 1180px, prose 720px.

### ★ 시그니처 모션 — 인용 마커 (이 프로젝트의 정체성)
역량 칩 옆에 `[N]` 위첨자 인용 마커(`.cite`, gold, pill). **호버하면 그 역량의 증거 텍스트가
툴팁으로 페이드인**됩니다. 이 모션이 제품의 핵심 상징 — 모든 화면에서 살려주세요.
```css
.cite { font-size:.65em; font-weight:500; color:var(--accent); background:var(--accent-bg);
        padding:1px 6px; border-radius:9999px; margin-left:4px; vertical-align:super; }
```

### 톤
아카이브·자산·신뢰. 정제되고 절제된 편집 디자인 느낌. **흔한 SaaS 대시보드 회피.**

---

## 3. 전체 유저 플로우 (설계해야 할 여정)

```
[방문자]
  홈(/) ─▶ 작동방식(/how-it-works) ─▶ 주요기능(/features) ─▶ 미리보기(/preview)
    │                                                              │
    └──────────────────── "시작하기" CTA ──────────────────────────┘
                                   ▼
                            로그인/회원가입(/login)
                                   ▼
                          ┌── 대시보드(/dashboard) ──┐
                          │   (비어있음: 빈 상태)     │
                          │        ▼                 │
                          │   FAB(+) 클릭             │
                          │        ▼                 │
                          │   링크 입력 모달          │
                          │   URL 붙여넣기 → 분석     │
                          │   (진행 3단계 표시)       │
                          │        ▼                 │
                          │   새 역량 카드 생성       │
                          │        ▼                 │
                          │   카드 클릭 → 상세(증거)  │
                          └──────────────────────────┘
```

핵심 전환점은 **대시보드에서 링크를 붙여넣어 첫 카드가 생기는 순간** — 여기가 "아하 모먼트"입니다.

---

## 4. 화면별 상세 요구

### 4-1. 홈 `/` (랜딩)
섹션 순서: **Hero**("활동 목록이 아니라, 증거 있는 역량 자산" + 증거 인용이 붙은 역량 카드 아트팩트)
→ **현재의 한계**(마일리지 PDF·자율입력 의존·역량진단 분리·교수상담 빈약, 4개)
→ **작동 방식**(경험 수집 → 증거 grounding → 역량 자산화, 3스텝)
→ **차별화**(기존 비교과 vs 우리, 대비 표)
→ **사용자**(학생/교수/관리자 3역할)
→ **신뢰의 토대**(증거 기반 추출·한국 표준 매핑·학생 데이터 통제권)
→ **FAQ** → **파트너십 문의** CTA.

### 4-2. 작동 방식 `/how-it-works`
3단계(수집 → 증거 grounding → 자산화)를 시각적으로. 실제 링크가 STAR로 분해되고
증거 인용이 하이라이트되는 과정을 보여주는 다이어그램/애니메이션.

### 4-3. 주요 기능 `/features`
핵심 기능 카드들: 링크 자동 분석 · 증거 인용 · K-CESA/NCS 매핑 · 역량 자산 카드 ·
(향후) AI 코칭 대화 · 자소서 초안. 각 기능에 짧은 설명 + 시각.

### 4-4. 미리보기 `/preview`
실제 대시보드/카드가 어떻게 보이는지 로그인 없이 맛보기. 샘플 역량 카드 2~3개 +
인용 마커 호버 데모. "직접 해보기" CTA → 로그인.

### 4-5. 로그인/회원가입 `/login`
- 이메일 + 비밀번호. **로그인/회원가입 토글**. 소셜(카카오/네이버/Google)은 **자리만**.
- 콘셉트 메시지: **"당신의 아카이브에 입장"** — 흔한 SaaS 로그인 화면 회피.
- 에러는 구체적으로. 비밀번호 요구사항 표시.

### 4-6. 대시보드 `/dashboard` (★ 가장 중요)
- **상단**: 인사 + 요약 통계(등록 경험 수, 추출 역량 수, 증거 개수 등 절제된 3~4개).
- **카드 그리드**: 각 카드 = 제목 · 기간 · 역할 · 추출 역량 칩 3개(+`[N]` 인용 마커) · 증거 개수 · 등록번호.
  카드 호버 시 subtle lift(translateY -2px). 카탈로그/도서 색인 카드 느낌.
- **빈 상태**: 카드가 없을 때 "첫 경험을 등록해보세요" + 화살표가 우하단 + 버튼을 가리킴.
- **FAB**: 우하단 플로팅 `+`(56px, gold accent, 항상 보임). 모바일에선 그대로, 모달은 bottom sheet.
- ★ 금지: 게이미피케이션 뱃지·레벨·진행률 게이지(마일리지 연상), 알림 배지 남발.

### 4-7. 링크 입력 모달 (대시보드 위)
- 오픈 시 **URL 입력에 자동 포커스**. URL만 필수, 나머지(제목·유형·기간)는 선택.
- "분석 시작"은 URL이 있을 때만 활성화. URL 필드에 **정규식 검증 금지**(붙여넣기 방해).
- 예시 링크 칩(GitHub/블로그 등) 제공.
- **진행 상태 3단계**(각 단계 명확히 노출, 무한 스켈레톤 금지):
  "링크에서 콘텐츠 가져오는 중… → AI가 경험 분해 중… → 역량 추출 중…"
- 완료 → 모달 닫히고 새 카드가 대시보드 상단에 페이드인. 모바일은 bottom sheet.

### 4-8. 카드 상세 (모달 또는 페이지)
- STAR 분해(Situation·Task·Action·Result) 표시.
- 각 역량 칩 + `[N]` 인용 마커 → 호버 시 **evidence 텍스트 + 출처(source_ref) 툴팁**.
- 등록번호·기간·원본 링크. (향후) AI 성찰 질문 자리.

### 4-9. 공통
- **네비게이션 바**: 모든 페이지 공통. 로고 + 6개 링크 + 로그인/대시보드 상태별 CTA.
- 반응형: 데스크톱(1180 container) / 태블릿 / 모바일(390). 모바일 내비는 햄버거 or 하단 탭.

---

## 5. 절대 하지 말 것 (안티패턴)

- ❌ 새 색·새 폰트 도입, 확정 토큰 훼손, 기본 회색(slate/gray) 남용
- ❌ 게이미피케이션(뱃지·레벨·XP·진행률 게이지), 챗봇 말풍선 UI, 알림 배지 남발
- ❌ 모션 남발 — **시그니처(인용 마커 페이드인) 외에는 절제**
- ❌ 증거 없는 역량을 카드에 표시 (모든 역량엔 인용 마커가 있어야 함)
- ❌ 흔한 SaaS 대시보드 클리셰

---

## 6. 산출물 형식 (이렇게 주세요)

1. **화면별 단일 HTML 파일** (또는 한 파일 안에 화면 전환) — 순수 HTML/CSS + 최소 JS.
   위 §2 토큰을 `:root`에 그대로 넣고, Newsreader/Pretendard 링크 포함.
2. 각 화면마다: 데스크톱 뷰 + 모바일(390px) 뷰가 확인되게.
3. 인용 마커 호버 툴팁 등 핵심 인터랙션은 실제로 동작하게(정적 이미지 X).
4. 시작 전에 **전체 플로우와 화면 목록을 먼저 요약**해 확인받고, 그다음 화면을 하나씩 만들어 주세요
   (한 번에 다 쏟아내지 말고 홈 → 로그인 → 대시보드 → 모달 순으로).

먼저 §3 플로우를 당신 방식으로 다시 정리해 이해가 맞는지 확인한 뒤, 홈 화면부터 시작해 주세요.
```
